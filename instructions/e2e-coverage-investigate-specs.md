`src/test/e2e` 配下の既存 spec を棚卸しし、台帳化に必要な入力情報を収集せよ。

**このムーブメントでは編集禁止。読み取り専用。**

## やること

1. `src/test/e2e/**/*.spec.ts` を列挙する
2. 以下の大分類候補で一次分類する（ファイル名ベースでよい）
   - STG実API資産候補
   - モックE2E候補
   - shared/helper/fixtures（台帳の対象外または補助）
   - 特殊用途（性能・表示検証・spweb等）
3. `spweb` 系、calendar 系、home/deceased CRUD 系など主要クラスタを抽出する
4. 初版で誤分類しやすい箇所を列挙する（中身再確認が必要）
5. `work/` 配下の既存関連文書（`e2e-test-strategy-notes.md` 等）があれば確認する

## 重要な観点（今回の課題を反映）

- **初版はファイル名ベースの粗分類を含んでよい**が、その限界を必ず明記する
- 既存テスト修正作業と衝突しないよう、**コード内容の変更提案に深入りしない**
- `spweb` 系・特殊用途 spec は、将来別台帳化の候補として識別する

## 必須出力

- spec 総数
- 大分類ごとの件数（概数可、根拠を明記）
- 主要クラスタ一覧
- 誤分類リスク / 要再確認ポイント

