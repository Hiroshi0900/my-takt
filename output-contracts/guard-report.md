# Output Contract: guard-report

## フォーマット

ガードレールレポートは以下の構造で出力する。

---

# ガードレールレポート: {タスク名}

## サマリー

| 重要度 | 件数 |
|------|------|
| 🔴 Critical（MUST FIX） | {数} |
| 🟠 High（MUST FIX） | {数} |
| 🟡 Warning（理由必須） | {数} |
| 🟢 Info | {数} |

**判定**: ✅ 承認 / ❌ 要修正 / ⚠️ 条件付き承認
**Doneゲート判定**:
- Critical未解消: {0 / N}
- High未解消: {0 / N}
- Warning未解消（許容理由なし）: {0 / N}
- 判定理由: {1-3行}

---

## 観点 1: Clean Architecture

### ✅ 問題なし / ❌ 違反あり

**チェック結果**:

| チェック項目 | 結果 | 詳細 |
|-----------|------|------|
| domain層の禁止import | ✅/❌ | {詳細} |
| ハンドラーのSafeNew*使用 | ✅/❌ | {詳細} |
| ファイル配置 | ✅/❌ | {詳細} |

**違反の詳細**（ある場合）:

🔴 **重大**: `{ファイルパス}:{行番号}`
```go
// 問題のあるコード
import "database/sql"  // domain層でのDB import禁止
```
**修正案**:
```go
// リポジトリI/Fを使う（interface_adapter層で実装）
```

---

## 観点 2: DDDパターン

### ✅ 問題なし / ❌ 違反あり

**チェック結果**:

| チェック項目 | 結果 | 詳細 |
|-----------|------|------|
| 2関数パターン (NewXxx + SafeNewXxx) | ✅/❌ | {詳細} |
| Specificationパターン | ✅/❌ | {詳細} |
| 1ファイル1型 | ✅/❌ | {詳細} |
| 命名（曖昧なサフィックスなし） | ✅/❌ | {詳細} |

**違反の詳細**（ある場合）:

🟡 **中程度**: `{ファイルパス}:{行番号}`
```go
// 問題: SafeNewXxxがない
func NewEmailAddress(email string) EmailAddress { ... }
// → 外部入力のバリデーションができない
```
**修正案**:
```go
func SafeNewEmailAddress(email string) (EmailAddress, error) {
    if err := EmailAddressSpec.IsSatisfiedBy(EmailAddress(email)); err != nil {
        return "", err
    }
    return EmailAddress(email), nil
}
```

---

## 観点 3: エラー処理

### ✅ 問題なし / ❌ 違反あり

**チェック結果**:

| チェック項目 | 結果 | 詳細 |
|-----------|------|------|
| DomainError実装 | ✅/❌ | {詳細} |
| コンパイル時チェック | ✅/❌ | {詳細} |
| FromError()マッピング | ✅/❌ | {詳細} |
| エラー握りつぶしなし | ✅/❌ | {詳細} |

**違反の詳細**（ある場合）:

🔴 **重大**: `{ファイルパス}:{行番号}`
```go
// 問題: エラーを握りつぶしている
_ = repo.Save(ctx, agg, events)
```
**修正案**:
```go
if err := repo.Save(ctx, agg, events); err != nil {
    return nil, fmt.Errorf("failed to save: %w", err)
}
```

---

## 観点 4: テスト

### ✅ 問題なし / ❌ 不足あり

**チェック結果**:

| チェック項目 | 結果 | 詳細 |
|-----------|------|------|
| ユースケーステスト存在 | ✅/❌ | {詳細} |
| 正常系テスト | ✅/❌ | {詳細} |
| 異常系テスト（バリデーション） | ✅/❌ | {詳細} |
| 異常系テスト（リポジトリエラー） | ✅/❌ | {詳細} |
| mockgenコメント | ✅/❌ | {詳細} |

**不足の詳細**（ある場合）:

🟡 **中程度**: `{ファイルパス}` - テスト不足
```
不足しているテストケース:
- 無効な{フィールド}のバリデーションエラー
- FindByIDがエラーを返すケース
```

---

## 観点 5: 要件トレーサビリティ（必須）

| 要件ID | 実装箇所（ファイル:行） | テストケース（ファイル:テスト名） | 判定 |
|--------|-------------------------|------------------------------------|------|
| {REQ-001} | `{path/to/file.go:123}` | `{path/to/file_test.go:TestXxx}` | OK / NG |

紐付け漏れ（ある場合）:
- {要件IDまたは不足内容}

---

## 観点 6: OpenAPI契約整合（必須）

| 操作 | 観点 | OpenAPI | 実装 | 判定 |
|------|------|---------|------|------|
| `{POST /v1/examples}` | 成功ステータス | 201 | 200 | NG |
| `{POST /v1/examples}` | 成功レスポンス型 | `ExampleResponse` | `ExampleDTO` | NG |
| `{POST /v1/examples}` | 失敗ステータス | 400/409 | 500 | NG |
| `{POST /v1/examples}` | 失敗レスポンス型 | `ErrorResponse` | `string` | NG |

差分サマリー:
- 差分件数: {0 / N}
- 未解消差分: {なし / 箇条書き}

---

## 観点 7: 実行ベース品質ゲート証跡（必須）

| 項目 | 実行コマンド | 結果 | 失敗時の対応方針 |
|------|--------------|------|------------------|
| build | `{プロジェクト標準のbuildコマンド}` | OK / NG | {対応方針} |
| test | `{対象テストコマンド}` | OK / NG | {対応方針} |
| lint | `{lintコマンド}` | OK / NG | {対応方針} |

---

## 観点 8: 再レビュー（前回指摘の解消確認）

| 指摘ID/要約 | 前回重要度 | 今回状態 | エビデンス |
|-------------|-----------|---------|-----------|
| {G-001 ...} | Critical / High / Warning | 解消 / 未解消 / 許容 | {diff/test/log} |

未解消指摘:
- {なし / 指摘一覧}

---

## 修正アクション

### 必須修正（次のイテレーションで対応）

1. **{観点}**: {修正内容}
   - ファイル: `{パス}`
   - 対応: {具体的な修正方法}

### 推奨修正

1. **{観点}**: {修正内容}

---

*レビュー完了。{件数}件の修正が必要。/ 問題なし、実装を承認。*
